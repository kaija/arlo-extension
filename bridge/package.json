{
  "name": "arlo-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Loopback host that runs the Codex agent for the Arlo extension.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@openai/codex-sdk": "^0.153.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "~5.9.3"
  }
}
